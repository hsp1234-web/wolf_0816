# Vue App 前端遷移後續驗證計畫

本文件旨在說明在完成從 `mp3.html` 到 Vue App 的前端遷移後，需要執行的手動與自動化驗證步驟。

由於在開發過程中，後端伺服器環境出現持續性的不穩定問題（具體表現為 `db_manager` 與 `api_server` 之間的競爭條件或日誌系統故障），導致無法在當前環境下完成端對端的 Playwright 驗證。

在後端穩定性問題解決後，應執行以下驗證步驟：

## 1. 啟動完整後端服務

使用 `runner/localrun.py` 腳本啟動完整的後端服務，並確保其穩定運行。

```bash
python runner/localrun.py --no-mock
```

## 2. 手動功能驗證

在瀏覽器中開啟新的 Vue 應用程式（URL 會由 `localrun.py` 提供），並執行以下操作流程，確保功能與舊版 `mp3.html` 一致：

- **[ ] 頁面載入**：確認應用程式主介面（包含上傳區塊、進行中任務、已完成任務）能正確顯示。
- **[ ] 檔案上傳**：
    - [ ] 透過點擊和拖曳方式，可以成功選擇一個或多個音訊檔案。
    - [ ] 檔案列表能正確顯示已選中的檔案。
    - [ ] 可以從檔案列表中移除檔案。
- **[ ] 任務生命週期**：
    - [ ] 點擊「開始處理」按鈕後，已選擇的檔案被清空。
    - [ ] 「進行中任務」列表中出現新的任務項目。
    - [ ] （後端正常運作時）任務狀態應能透過 WebSocket 即時更新。
    - [ ] 任務完成後，項目應從「進行中」列表移動到「已完成」列表。
- **[ ] 已完成任務操作**：
    - [ ] 點擊「下載」按鈕，應能成功下載對應的結果檔案。
    - [ ] 點擊「預覽」按鈕，應能觸發預覽功能（目前為 `alert` 佔位符）。

## 3. Playwright 自動化驗證 (建議)

在手動驗證通過後，建議編寫一個 Playwright 測試腳本，以自動化上述核心流程，用於未來的功能迴歸測試。

## 備註

- 本次遷移**未修改**位於 `src/static/mp3.html` 的原始檔案，該檔案可繼續作為功能比較的基準。
- 本次遷移已將所有前端相關的程式碼、樣式和業務邏輯（部分）轉移至 `vue-app` 目錄中，並建立了基於 Pinia 的狀態管理架構。

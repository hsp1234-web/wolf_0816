# ================================================================
# "輕量測試模式" 所需的依賴
# "輕量"體現在安裝CPU版torch和使用tiny模型，而非減少套件
# 版本號已根據用戶要求鎖定
# ================================================================

# --- 核心 Web 框架 ---
fastapi==0.110.0
uvicorn==0.29.0

# --- 異步 HTTP 客戶端 (用於 TestClient) ---
httpx==0.27.0

# --- FastAPI 依賴 ---
python-multipart==0.0.9
pytz==2024.1

# --- 其他核心依賴 ---
jsonpatch==1.33
requests==2.31.0
websockets==12.0
pydantic-settings==2.2.1
psutil==5.9.8

# --- 測試框架 ---
pytest==8.1.1

# --- AI/ML 轉錄模型相關 ---
# 注意：torch將透過 --index-url 從cpu源安裝
numpy<2.0  # JULES'S FIX (2025-08-28): 鎖定 NumPy 版本以避免與 torch/ctranslate2 的 ABI 衝突
torch==2.2.2
faster-whisper==1.0.1
opencc-python-reimplemented==0.1.7

# 前端遷移後續驗證狀態報告

**狀態更新日期:** 2025年8月17日

## 摘要

本文件旨在取代舊版的**手動驗證計畫**。隨著後端服務的穩定以及端對端 (E2E) 測試的成功修復，原先計畫中大部分的手動驗證步驟現已**完全自動化**。

目前，專案的正確性主要由位於 `e2e_tests/` 目錄下的 Playwright 測試套件來保障。

## 自動化測試涵蓋範圍

現有的 E2E 測試套件涵蓋了以下核心使用者流程：

1.  **基本頁面互動 (`test_basic_flow.py`)**
    *   驗證應用程式能否成功載入。
    *   驗證頁面標題是否正確。
    *   驗證使用者能否在不同功能分頁之間進行切換。

2.  **多階段任務生命週期 (`test_multi_stage_task_lifecycle.py`)**
    *   驗證一個由多個後端任務組成的完整流程（例如 YouTube 下載 -> AI 分析）在前端的 UI 反應。
    *   測試會模擬後端發送的 WebSocket 更新，並斷言前端 UI 能正確地將任務從「進行中」列表移至「已完成」列表。
    *   **註**：此測試已根據前端現有的 bug 進行了調整，使用 `task_id` 而非任務標題來識別 UI 元件。

3.  **特殊字元檔名處理 (`test_preview_with_special_characters.py`)**
    *   此測試旨在驗證前端能否正確處理並預覽包含 `?`、`#` 等特殊字元的檔名。
    *   **目前狀態：已跳過 (Skipped)**
    *   **原因**：此測試揭露了一個前端的 Bug — 前端在請求預覽內容時，未對 URL 進行必要的編碼，導致後端回傳 404 錯誤。因該問題無法僅從測試端修復，故暫時跳過此測試，以待前端開發人員修復。

## 如何執行自動化驗證

所有開發者在提交程式碼變更前，應執行完整的 E2E 測試套件，以確保沒有引入新的迴歸錯誤。

**執行步驟:**

1.  **建置前端 (若有變更)**
    ```bash
    cd vue-app
    bun run build
    cd ..
    ```

2.  **執行測試腳本**
    ```bash
    python3 runner/localtest.py
    ```

腳本將會自動啟動所有必要的服務、執行測試，並在完成後輸出結果報告。

## 結論

專案的前端遷移驗證工作已從手動階段，成功過渡到由自動化 E2E 測試保障的階段。這為專案未來的迭代和維護提供了堅實的品質基礎。

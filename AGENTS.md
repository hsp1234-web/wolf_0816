# AI 代理開發準則

本文件概述了開發與測試此專案的具體要求和限制。所有在此儲存庫中工作的 AI 代理都必須遵守這些準則。

---

## 2. ⚠️ 極度重要的環境限制與安全操作準則 (CRITICAL Environment Limitations & Safe Operating Procedures)

**背景**: 經過診斷，發現此沙箱環境在特定條件下存在一個會導致**永久性鎖死**的嚴重 Bug。所有 AI 代理都**必須**遵守以下準則，以避免觸發此問題。

**觸發條件**:
*   在檔案系統的根目錄或子目錄中**建立新的資料夾**。

**核心原因 (假設)**:
*   環境內的一個檔案系統監控服務存在**競爭條件 (Race Condition)**。它在收到目錄建立通知時，會錯誤地將新目錄當作檔案處理，並嘗試執行 `cat` 指令，從而導致指令失敗並使整個沙箱崩潰。

**安全操作準則 (MANDATORY)**:

*   **🚫【絕對禁止】建立新目錄**: 在任何情況下，都不要使用 `mkdir` 或任何會建立新目錄的腳本/程式碼。
*   **🚫【絕對禁止】執行會自動建立目錄的測試**: 嚴禁執行 `runner/run_colabpro_test.py` 或任何其他已知會在檔案系統上建立新目錄的測試腳本。
*   **✅【建議作法】使用現有目錄**: 如果需要暫存檔案，請一律使用系統中既有的、安全的目錄（例如 `/tmp`）。
*   **✅【建議作法】小心使用檔案操作工具**: 在使用 `create_file_with_block`、`rename_file` 等工具時，要格外小心，確保其操作不會間接觸發新的目錄建立。
*   **✅【開發流程】優先靜態分析**: 在修改程式碼後，應優先使用 `read_file` 來驗證變更，而不是直接執行程式碼。只有在絕對必要且確認操作安全時，才能執行。

**違反以上準則極有可能導致您失去對環境的控制權，且無法恢復。請務必嚴格遵守。**

---

## 1. 語言與溝通要求 (Language and Communication)

**❗❗❗ 極度重要 (CRITICAL) ❗❗❗**

**全程必須使用「繁體中文」進行溝通。**

這包括但不限於：
*   **使用者訊息**：所有與使用者的互動和回覆。
*   **程式碼註解**：所有新撰寫或修改的程式碼註解。
*   **提交訊息**：Git 的提交標題和內文。
*   **技術文件**：所有 `.md` 檔案的更新。
*   **日誌與輸出**：在不影響程式運作的前提下的所有輸出訊息。

此要求是專案的最高優先級之一，請務必遵守。

-
---

每次提交必須修改寫入CHANGELOG.md，並且要加上ISO的詳細時間戳記使用台北亞洲的時間

 程式碼庫歷史背景 (Codebase Context)

 Vue.js 應用所需的新邏輯。這項工作已經完成。

DOC/ARCHITECTURE_RESEARCH.md可參考

請使用繁體中文計畫全程使用繁體中文進行溝通。 最終產出的 GitHub 敘述文件、程式碼註解及輸出訊息，在不影響程式運作的前提下，都要是繁體中文。

# AGENTS.md - 給 AI Agent 的說明

歡迎，AI Agent！這份文件將幫助你了解如何在這個專案中工作。

## 專案結構

- `src/`: 核心應用程式原始碼。
- `runner/`: 包含用於執行測試或本地任務的啟動器腳本。
- `e2e_tests/`: 包含 Playwright 端對端測試案例。

## 如何執行端對端 (E2E) 測試

為了確保你的變更沒有破壞前端或核心後端流程，你**必須**在提交前執行端對端測試。

我們已經建立了一個自動化的測試啟動器來簡化這個流程。

**執行指令:**

```bash
python3 runner/localrun.py
```

**這個指令會做什麼？**

1.  **自動安裝依賴**: 它會檢查並安裝 `requirements-server.txt` 中定義的必要 Python 套件。
2.  **啟動後端服務**: 它會以正確的順序啟動資料庫管理器和 API 伺服器。
3.  **執行 Playwright 測試**: 服務就緒後，它會自動執行 `e2e_tests/` 目錄下的所有測試案例。
4.  **監控與超時**:
    - **看門狗**: 如果服務在 15 秒內沒有任何日誌輸出，流程將會中止。
    - **全局超時**: 整個測試流程被限制在 130 秒內完成。
5.  **自動清理**: 測試結束後，它會自動關閉所有啟動的服務。

**預期結果:**

如果指令成功執行並以返回碼 `0` 結束，同時日誌中顯示 `🎉 測試流程圓滿成功！`，則表示你的變更沒有破壞主要的端對端流程。

**在真實模式下測試:**

如果你需要模擬更真實的環境 (例如，不使用 mock transcriber)，可以加入 `--no-mock` 旗標：

```bash
python3 runner/localrun.py --no-mock
```

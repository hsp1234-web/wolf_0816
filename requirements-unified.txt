# ================================================================
# 統一後端服務 (`src/main.py`) 及其測試環境所需的依賴
# ================================================================

# --- 核心 Web 框架 ---
fastapi
uvicorn

# --- 背景任務佇列 (已移除 Huey) ---

# --- 異步 HTTP 客戶端 (用於 TestClient) ---
httpx

# --- 測試框架 ---
pytest
trio

# --- FastAPI 依賴 ---
python-multipart
pytz

# --- 其他核心依賴 ---
jsonpatch
requests
websockets
pydantic-settings
psutil # 硬體監控工作者需要

# ================================================================
# 統一後端服務 (`src/main.py`) 及其測試環境所需的依賴
# ================================================================

# --- 核心 Web 框架 ---
fastapi
uvicorn

# --- 背景任務佇列 ---
huey

# --- 異步 HTTP 客戶端 (用於 TestClient) ---
httpx

# --- 測試框架 ---
pytest

# --- FastAPI 依賴 ---
python-multipart  # 用於表單資料和檔案上傳
pytz              # 用於日誌和時區處理

# --- 其他核心依賴 ---
jsonpatch         # 用於 state_manager 的狀態差異計算

# 開發計畫與修改日誌 (繁體中文)

**最後更新時間:** 2025-08-19

## 當前計畫 (Current Plan)

### 任務：建立一個獨立的、可計時的啟動測試引擎

- **目標**: 根據使用者要求，建立一個單一的、獨立的測試腳本，該腳本能夠在 60 秒內完成環境設定、依賴安裝、服務啟動和 UI 驗證的完整流程。
- **狀態**: 🟡 **進行中**

### 技術選型決策 (2025-08-19)

為了打造一個符合要求的、可擴展的「測試引擎」，我們對幾種技術方案進行了研究與比較。

| 方案 | 代表套件 | 優點 | 缺點 | 推薦指數 |
| :--- | :--- | :--- | :--- | :--- |
| **A. 純標準庫** | `subprocess`, `threading`, `venv` | - **零依賴**：腳本本身不需安裝任何額外套件。<br>- **完全可控**：每個細節都由我們自己編寫。 | - **程式碼繁瑣**：處理指令的輸出、錯誤和超時會需要大量樣板程式碼。<br>- **可讀性差**：邏輯複雜，不易閱讀和維護。 | ★★★☆☆ |
| **B. 指令執行優化** | `sh` | - **語法極簡**：像寫 Shell 腳本一樣執行指令 (`sh.uv(...)`)，非常優雅。<br>- **輸出處理簡單**：輕鬆獲取指令的輸出和返回碼。 | - **功能單一**：主要只解決「執行指令」的問題，對流程控制幫助不大。 | ★★★★☆ |
| **C. 任務執行框架** | `invoke` | - **專為任務打造**：天生就是用來定義和執行任務流程的（例如 `install`, `build`, `test`）。<br>- **結構化**：能將引擎內的每個步驟定義成獨立的函式，邏輯清晰，非常符合「測試引擎」的概念。<br>- **可擴展性強**：未來要整合更多端對端測試會非常容易。 | - **輕量級框架**：雖然它本身是個小框架，但我們可以把所有任務都寫在同一個檔案中，符合您的「單一檔案」要求。 | ★★★★★ |

**最終決定**: 我們選擇**方案 C (`invoke`)**。`invoke` 讓我們能以清晰、結構化的方式定義測試引擎的每一步，使其易於理解和擴展，完全符合建立一個可維護、功能強大的測試引擎的長期目標。

---

## 開發摘要 (2025-08-19)

### 任務：將 `Colabpro.py` 設定為完整模式啟動器

- **目標**: 根據使用者要求，將 `Colabpro.py` 設定為一個能夠下載並運行所有真實依賴和工作者的「完整模式」啟動器，並將此規則寫入 `AGENTS.md` 作為例外。
- **狀態**: ✅ **已完成**

#### 主要變更
1.  **`Colabpro.py` 修改**:
    - 移除了強制設定 `API_MODE='mock'` 的邏輯，使其以真實模式運行。
    - 恢復了 `requirements-worker.txt` 的依賴安裝，確保所有重量級工作者的依賴都被下載。
2.  **`AGENTS.md` 更新**:
    - 新增了例外規則，明確指出 `Colabpro.py` 不受模擬（mocking）限制。
    - 強調其設計目標是作為一個功能齊全的線上驗證環境。

#### 提交紀錄
- 上述變更已在 `feat/colabpro-full-mode` 分支中提交。
- 相關的 `plan.md` 更新已在 `docs/add-plan-summary` 分支中提交。

---

## 歷史開發紀錄 (2025-08-19 之前)

### 核心目標 (舊)

本次開發旨在解決使用者回報的三個主要問題：
1.  **伺服器啟動緩慢**: 尤其是在 Colab 環境中，從取得連結到服務可用時間過長。
2.  **初始載入失敗**: 由於啟動緩慢，使用者在服務就緒前提早點擊連結會看到錯誤頁面。
3.  **前端工作者狀態顯示錯誤**: Vue 前端的工作者狀態燈號，一直顯示為「準備中」，無法正確反映後端就緒狀態。

### 採用的核心策略：預烘烤環境 (Pre-baked Environment)

經過分析，我們發現啟動緩慢的主要瓶頸在於每次啟動時都必須完整執行 `pip install` 和 `npm install && npm run build`。為此，我們採納了「預烘烤」策略來從根本上解決此問題。

#### 策略原理：
- **一次建置，多次使用**: 將耗時的依賴安裝與前端建置工作，從「每次啟動都執行」變為「只需執行一次」。
- **打包與分發**: 將安裝好的後端 Python 依賴 (`site-packages`) 和建置好的前端成品 (`dist` 目錄) 分別壓縮成 `.zip` 檔。
- **雲端儲存**: 使用者可以將這些壓縮檔上傳至 Google Drive 等雲端空間。
- **智慧啟動流程**:
    1.  **快速路徑 (Happy Path)**: 應用程式啟動時，會優先嘗試從 Google Drive 下載預烘烤的壓縮檔，並直接解壓縮使用。這將數十秒的安裝編譯時間縮短為幾秒鐘的 I/O 操作。
    2.  **後備機制 (Fallback)**: 如果下載失敗 (例如連結失效、網路問題)，程式**不會崩潰**，而是會自動切換回傳統的、較慢的安裝流程。
    3.  **自我修復 (Self-Healing)**: 當後備的傳統安裝成功後，程式會自動在本地產生一份全新的、可用於上傳的預烘烤壓縮檔，方便使用者更新雲端上的版本。

### 已完成的修改與開發 (舊)

#### a. 建立打包腳本 (`scripts/build_artifacts.sh`)
- 我已建立一個 shell 腳本，它會自動化執行以下所有打包工作：
    - 安裝 `requirements-server.txt` 中所有的後端依賴到一個臨時目錄。
    - 將後端依賴目錄壓縮成 `artifacts/backend-deps.zip`。
    - 進入 `vue-app` 目錄，執行 `npm install` 和 `npm run build`。
    - 將建置好的 `dist` 目錄壓縮成 `artifacts/frontend-dist.zip`。

#### b. 改造啟動流程 (`Colab.py`)
- 我重構了 `Colab.py` 中的 `BackgroundWorker` 類別，實現了上述的智慧啟動邏輯：
    - 新增 `_try_prebake_setup` 方法，專門處理快速啟動路徑。
    - 新增 `_bake_new_artifacts` 方法，用於在後備安裝成功後，自動執行打包腳本。
    - 修改了主執行函數 `_run`，使其具備決策能力，優先嘗試預烘烤，失敗則優雅降級。
    - 為了支援從 Google Drive 下載，已在啟動環境中自動安裝 `gdown` 套件。

#### c. 建立效能測試腳本 (`runner/test_startup_performance.py`)
- 為了量化優化成果，我建立了一個專門的效能測試腳本，它可以：
    - 模擬 Colab 環境，以程式化方式運行 `Colab.py` 啟動器。
    - 分別測試「標準安裝」和「預烘烤」兩種模式下的啟動時間。
    - 透過讀取日誌資料庫，精確計算從啟動到服務就緒所需的時間。
    - 自動產生以下的效能對比報告。

### 預期與實測效益 (舊)

#### 效能對比測試結果

| 啟動模式 (Startup Mode) | 所需時間 (Time Taken) | 備註 (Notes) |
| :---------------------- | :-------------------- | :------------- |
| 標準安裝 (Normal Install) | 64.57 秒              | 每次都需重新安裝依賴與建置前端 |
| 預烘烤策略 (Pre-baked)  | 10.46 秒              | 直接使用預先打包的檔案，僅解壓縮 |
| **節省時間 (Time Saved)** | **54.12 秒 (~84%)** |                |

**結論**: 實測證明，預烘烤策略極大地改善了啟動效能，**成功將啟動時間縮短了 84%**，從超過一分鐘縮短至約 10 秒，有效解決了「啟動緩慢」和「初始載入失敗」的核心問題。

### 目前進度與後續步驟 (舊)

- ✅ **完成**: 後端預烘烤、後備安裝、自我修復的完整邏輯開發。
- ✅ **完成**: 建立並執行了效能對比測試，驗證了優化的大幅成效。
- 🟡 **進行中**: 正在偵錯「前端工作者狀態顯示錯誤」的問題。
    - **已發現**: 前後端的狀態更新邏輯表面上看起來是匹配的，但最終的 `READY` 狀態訊息似乎未能成功在前端觸發更新。
    - **已修改**: 我已在後端 `api_server.py` 的廣播邏輯中加入額外的偵錯日誌。
    - **下一步**: **需要使用者協助**，重新運行應用程式並提供新的日誌，以便我根據偵錯日誌定位問題根源。在收到日誌後，我將繼續完成此問題的修復。

## 進度追蹤 (Progress Tracker)

- [x] **第一階段：建立「基礎設施核心」** (已完成)
    - [x] `建立所有微服務的基礎框架` (已完成)
    - [x] `實作 檔案管理服務 (File Management Service) 核心邏輯` (已完成)
    - [x] `實作 AI 本地模型服務 (AI Local Model Service) 核心邏輯` (已完成)
    - [x] `實作 日誌管理服務 (Log Management Service) 核心邏輯` (已完成)
- [x] **第二階段：建構「核心轉錄流程」** (已完成)
- [ ] **第三階段：擴充功能與完善系統** (下一步)

---

# 微服務架構遷移作戰計畫

## 1. 總體目標

本計畫旨在根據 **`research_0818.md`** 文件中的藍圖，將現有後端系統逐步遷移至一個高度解耦的微服務架構。

核心原則如下：
- **新舊並行**：在不改動現有舊架構的前提下，獨立開發與測試新的微服務。
- **安全第一**：確保新架構經過完整測試並驗證穩定後，才會將舊有程式碼進行「封存」。
- **隨時回滾**：整個過程應支援在必要時能輕鬆回滾至舊架構。

---

## 2. 參考文件

所有開發工作都應嚴格遵循以下文件的指導：
- **主要藍圖**: `research_0818.md`

---

## 3. 技術選型與策略

根據我們深入的討論，我們將採用以下技術與策略：

- **服務間通訊**:
    - **非同步通訊 (主要)**: 使用 `huey` 任務佇列作為系統的非同步訊息骨幹，用於處理不需要即時回應的命令與事件，以提升系統穩定性與可靠性。
    - **同步通訊 (輔助)**: 對於需要即時回應的請求（例如：服務間的資料查詢），將為服務建立基於 `FastAPI` 的輕量級 REST API。

- **設定管理**:
    - 採用 **環境變數** 搭配 **`.env`** 檔案的方式。每個微服務將有自己獨立的 `.env` 檔案來管理開發環境的設定，確保設定與程式碼分離，並提升安全性與靈活性。

- **測試策略**:
    - **(已暫停)** 根據最新指示，暫時擱置所有測試活動，優先完成所有服務的功能性建置。未來將再重新評估並補上測試。

- **舊程式碼處理**:
    - 當所有新服務上線並穩定運作後，舊的相關程式碼將被移動到一個新建的 **`_archive/`** 目錄中進行封存，而非直接刪除。

---

## 4. 分階段實施計畫

我們將採用三階段計畫來逐步完成遷移：

### **第一階段：建立「基礎設施核心」 (當前階段)**

此階段的目標是建立整個系統最根本、被其他服務依賴的基石。

**要開發的服務**:
1.  **`檔案管理服務 (File Management Service)`**: 負責所有檔案的儲存、讀取、刪除等操作。提供 REST API 供其他服務呼叫。
2.  **`AI 本地模型服務 (AI Local Model Service)`**: 封裝 `torch`、`faster-whisper` 等重度依賴。提供一個簡單的 REST API，接收檔案路徑並回傳分析或轉錄結果。
3.  **`日誌管理服務 (Log Management Service)`**: 建立一個中央日誌中心，供所有其他服務傳送日誌。

### **第二階段：建構「核心轉錄流程」**

在基礎設施穩定後，串接第一個完整的核心業務流程。

**要開發的服務**:
1.  `轉錄任務服務 (Transcription Task Service)`
2.  `API 閘道 (API Gateway)`
3.  `靜態網頁伺服器 (Static Web Server)`

### **第三階段：擴充功能與完善系統**

擴充其他業務功能，並完善使用者體驗。

**要開發的服務**:
1.  `YouTube 下載服務 (YouTube Download Service)`
2.  `AI 報告服務 (AI Report Service)`
3.  `通知服務 (Notification Service)`
4.  `媒體預覽服務 (Media Preview Service)`

---

本計畫將作為我們後續開發工作的正式指導文件。

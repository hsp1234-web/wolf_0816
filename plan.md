# 工作者改造計畫 (Worker Refactoring Plan)

**核心設計原則：**
*   **單一檔案，自我管理**：每個工作者都是一個獨立的 `.py` 檔案，能夠自我管理虛擬環境和依賴，確保在任何地方都能一致地執行。
*   **穩定性優先**：採用單線程處理模型，確保任務一個接一個執行，並具備完整的錯誤處理和重試機制。
*   **邊界清晰**：每個工作者只負責一種類型的任務，保持架構的簡潔與單一職責。
*   **可測試性**：每個工作者都應提供簡單的方式來觸發測試任務，方便開發和驗證。

---

### **第一階段：概念驗證 - 改造 YouTube 服務**

**狀態：** ✅ **已完成**

**目標：** 建立第一個符合新規範的獨立工作者，並驗證其可行性。

**實作摘要：**
*   成功建立了一個獨立的 `run_youtube_worker.py` 檔案，取代了舊的微服務。
*   此腳本能使用 `uv` 自我管理虛擬環境與鎖定版本的依賴。
*   實現了使用 `yt-dlp` 的真實下載邏輯，並具備完整的錯誤處理。
*   包含了閒置 20 秒後自動關閉的功能，以節省系統資源。
*   日誌系統已升級為使用繁體中文和亞洲/台北時區的 ISO 8601 格式。
*   舊的 `services/youtube_service` 目錄已被成功清理。
*   透過 `--test` 參數提供了內建的測試模式。

---

### **第二階段：推廣至其他微服務**

**狀態：** ✅ **已完成**

**目標：** 將成功的模式複製到其他背景服務，完成架構的全面轉型。

**實作摘要：**
*   成功建立了 `run_transcription_worker.py` 和 `run_ai_report_worker.py` 兩個獨立工作者。
*   遵循 `AGENTS.md` 的嚴格規定，透過 `time.sleep` 和產生固定的假資料，成功模擬了 `faster-whisper` 和 `google-generativeai` 的行為，避免安裝大型 AI 依賴。
*   實現了從轉錄任務到 AI 報告任務的自動化鏈式觸發。
*   所有新工作者均採用了與 YouTube 工作者相同的自我管理 (`uv`)、日誌和閒置關閉架構。
*   已成功清理舊的 `services/transcription_service` 和 `services/ai_report_service` 目錄。

---

### **第三階段：文件與最終化**

**狀態：** ✅ **已完成**

**目標：** 確保專案的知識得以傳承，方便未來維護。

**實作摘要：**
*   已更新專案根目錄的 `README.md`，詳細說明了「獨立工作者」的設計理念，並提供了啟動每個新工作者的操作指南。
*   所有相關變更已提交至版本控制。

---
### **最終狀況更新 (2025-08-18)**

**問題**：
已成功解決「幽靈程式碼」問題。該問題導致應用程式在啟動時，即使程式碼中已移除 Redis 依賴，仍然會錯誤地安裝 `redis` 套件並嘗試連接 Redis 伺服器。

**根本原因**：
問題的根源被確定為陳舊的 Python 位元組碼快取 (`.pyc` 檔案)。過時的 `.pyc` 檔案中包含了舊的、依賴 Redis 的程式碼邏輯，而 Python 直譯器為了效能會優先執行這些快取檔案，從而忽略了更新後的 `.py` 原始碼。

**解決方案**：
修改了主啟動腳本 `runner/localrun.py`，在其中加入了一個自動清理機制。現在，每次啟動應用程式時，該腳本會先遞迴地刪除專案中所有的 `__pycache__` 目錄。

**驗證**：
此修復方案已經通過了專案完整的 E2E 測試套件 (`runner/localtest.py`) 的驗證，所有測試均成功通過。

**結果**：
此問題已徹底解決。程式碼已提交，分支名稱為 `fix/ghost-code-pycache-issue`。

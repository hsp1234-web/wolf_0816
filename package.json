{
  "name": "/app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "snapshot": "bun install && mkdir -p logs && python3 -m pip install -r requirements-server.txt && npx playwright install chromium && bun scripts/snapshot.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/bun": "latest",
    "finalhandler": "^2.1.0",
    "http-server": "^14.1.1",
    "jsdom": "^26.1.0",
    "playwright": "^1.45.0",
    "serve-static": "^2.2.0",
    "ws": "^8.18.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "wav": "^1.0.2"
  }
}

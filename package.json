{
  "name": "e2e-test-runner",
  "version": "1.0.0",
  "description": "A minimal package to run Playwright for E2E tests.",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.55.0"
  }
}
